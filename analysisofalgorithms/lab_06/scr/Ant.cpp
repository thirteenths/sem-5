
#include "Ant.h"

/*Ant::Ant(int count)
{
    way = new bool[count];

    wayCount = count;

    route = new int[count];
    //route[0] = n;
    routeCount = count - 1;
    
}

Ant::~Ant()
{
    delete []way;
    delete []route;
}

void Ant::generateAnts(int n)
{

    for(int i = 0; i < wayCount; i++)
        way[i] = true;
    way[n - 1] = false;
    route[0] = n - 1;
}

void Ant::choiceNextCity(Matrix &pheromones, Matrix &A, double alpha, double beta)
{
    double numerator = 0;
    double denominator = 0;

    double tao, reverseCost;
    int cost;

    int cityCurr = wayCount;

    for (int i = 0; i < routeCount; i++)
    {
        
    }
}
*/